# Evidence for main 870726c

Captured by `bun run verify:features` on 2026-09-09. `report.json` lists each gate,
its exit status, and its output file. The YAML is generated by the main plugin;
several files are intentionally invalid against the live operator schema and
are evidence of release blockers, not deployment templates to copy.

ANSI colors and trailing whitespace were removed. `$FRAMEWORK` and `$EXAMPLES` replace workstation paths.
The baseline smoke log describes existing components from the previous verified
build, not the new candidate apps. No candidate passed both build and server
validation, so none proceeded to deployment.
